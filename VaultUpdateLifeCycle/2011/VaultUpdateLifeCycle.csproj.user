<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <StartArguments>-server localhost -vault Houchin -username Administrator -password gorillas -lifecycledef "Houchin Release Process" -statee Released -noforce</StartArguments>
  </PropertyGroup>
</Project>